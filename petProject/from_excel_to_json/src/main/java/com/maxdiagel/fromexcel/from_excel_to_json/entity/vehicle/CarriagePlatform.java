package com.maxdiagel.fromexcel.from_excel_to_json.entity.vehicle;

public class CarriagePlatform extends Carriage implements Vehicle{

    public CarriagePlatform(String number, VehicleType type) {
        super(number, type);
    }
}
