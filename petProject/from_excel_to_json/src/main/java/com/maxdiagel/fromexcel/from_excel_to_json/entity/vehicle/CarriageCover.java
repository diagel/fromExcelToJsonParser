package com.maxdiagel.fromexcel.from_excel_to_json.entity.vehicle;

public class CarriageCover extends Carriage implements Vehicle{

    public CarriageCover(String number, VehicleType type) {
        super(number, type);
    }
}
