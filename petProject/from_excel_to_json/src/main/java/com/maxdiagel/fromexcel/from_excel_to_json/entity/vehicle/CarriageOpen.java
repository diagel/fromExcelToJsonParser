package com.maxdiagel.fromexcel.from_excel_to_json.entity.vehicle;

import java.lang.reflect.Field;

public class CarriageOpen extends Carriage implements Vehicle{

    public CarriageOpen(String number, VehicleType type) {
        super(number, type);
    }
}
